// Keep the versioned path as a compatibility alias. New callers should use the
// public namespace because the catalogue is intentionally unauthenticated.
export { GET } from '../public/model-pricing'
